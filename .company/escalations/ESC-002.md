# ESC-002 — Decide remediation priority for two HIGH CISO findings that together mean company-os's core promise — 'gates that cannot be self-certified' — does not currently hold: (1) the Evidence Rule's gate check only verifies actor != owner, not actor role, and $COMPANY_ACTOR is trivially overridable by any Bash-capable worker, so a worker can forge its own REVIEW_PASSED/QA_PASSED/SECURITY_REVIEW_PASSED with zero real review (demonstrated live). (2) risk-triggers.yaml has no coverage for changes to the guardrail mechanism itself (hooks/, config/quality-gates.yaml, config/risk-triggers.yaml, worker.py's tool wiring), so a task editing the enforcement code can ship with only a single reviewer and no security gate.

- **Status**: open  ⛔ blocking
- **Kind**: risk
- **Raised by**: company-pm at 2026-08-16T18:06:53Z

## Context

Both are structural, not per-task — they affect every project company-os orchestrates, not just this repo, and neither would ever surface from a single task's diff review (that is why they only turned up in a whole-repo CISO pass). README.md already names the first as a known-unenforced gap from a real prior incident ('a real reviewer run signed itself qa/verifier') but no fix has landed. Options: (a) fix now before company-os is trusted on any other repo — bind gate-event actors to WORKER_STARTED launch records (cheap, scoped) and add a governance-path trigger to risk-triggers.yaml (config-only change); (b) accept the risk short-term for solo/trusted-operator use (Riyan is currently both PM and the only human in the loop, so self-certification risk is lower in practice than in a multi-operator setting) and schedule the fix before onboarding company-os onto any repo with other contributors or before delegating more autonomy to Tier-2 workers. Either way this needs your call, not mine — accepting or deferring this risk is irreversible in the sense that every gate passed before the fix has no real guarantee behind it.

## To resolve

```bash
company resolve ESC-002 --resolution "..."
```
